# 待办

已完成的工程骨架、资源同步、安装/卸载流程、安全路径控制、最小 UI 和 bundle resources 见 [IMPLEMENTATION_STATUS.md](./IMPLEMENTATION_STATUS.md)。本文件只保留尚未完成或尚未在目标环境验收的工作。

## 本轮重构：在线控件、插件管理、权限管理与用户手册

> 当前阶段只完成需求拆分与 UI 设计。UI 审核通过后再进入开发，避免在交互方案未确认前改动安装器核心流程。

### P0：先确认产品与数据契约

- [ ] 确认“控件源”清单格式：源名称、索引 URL、是否启用、优先级、健康状态和最后同步时间；
- [ ] 定义控件索引格式：控件 ID、名称、版本、平台、下载 URL、SHA-256、包大小、发布时间、说明和最低 WPS 版本；
- [ ] 默认内置一个官方控件源，仅内置源地址和信任信息，不再把日期选择器压缩包打进安装器；
- [ ] 确定默认托管服务与备用镜像，下载必须使用 HTTPS、版本固定 URL 和 SHA-256 校验；
- [ ] 明确自定义源的校验规则、超时、重试、失效提示、同名控件冲突和源优先级；
- [ ] 明确从旧版“内置日期选择器”迁移到在线源后的离线行为和兼容策略；
- [ ] 定义已安装插件的发现规则，区分“本应用安装”“外部安装”“残留/损坏”三类状态；
- [ ] 定义卸载边界：只移除用户明确选择的插件，保留其他 `jsaddons` 内容及无关 `publish.xml` 条目；
- [ ] 定义 macOS 与 Windows 的权限状态模型、重新申请能力和无法直接申请时的系统设置指引。

### P0：UI 与交互审核

- [ ] 审核信息架构：插件、控件源、权限、设置/帮助四个入口；
- [ ] 审核“插件”页：已安装/可安装列表、搜索、状态、版本、来源、安装/更新/卸载和批量选择；
- [ ] 审核“控件源”页：默认源、自定义源、新增/编辑/删除、启停、测试连接和刷新；
- [ ] 审核“权限”页：访问目标、当前状态、申请/重新申请、打开系统设置、选择 WPS 目录和故障指引；
- [ ] 审核确认弹窗、下载/校验/安装进度、错误恢复、空状态和无网络状态；
- [ ] 审核用户手册结构与应用内帮助入口；
- [ ] 审核通过后冻结首版范围，再切换开发模型实施。

### P1：控件源与下载模块

- [ ] 移除 `src-tauri/resources/addon/wps-addon-build/date-picker.7z` 的打包依赖；
- [ ] 新增控件源配置存储，支持默认源和多个用户自定义源；
- [ ] 新增源索引拉取、格式校验、缓存、刷新、超时和错误分类；
- [ ] 新增下载队列、进度、取消、临时文件、SHA-256 校验和失败清理；
- [ ] 禁止路径穿越、符号链接逃逸、非预期文件覆盖和未校验包安装；
- [ ] 支持源启停、排序、测试连接、恢复默认源和缓存清理；
- [ ] 将现有日期选择器发布为默认源中的首个控件，并验证冷启动安装流程；
- [ ] 为无网络、源不可达、索引损坏、校验失败、磁盘空间不足和下载中断增加测试。

### P1：插件管理模块

- [ ] 新增已安装插件扫描与标准化数据模型；
- [ ] 展示插件名称、ID、版本、类型、安装来源、安装路径、健康状态和可用更新；
- [ ] 支持单选/多选卸载，操作前展示影响范围，操作后刷新状态；
- [ ] 支持安装、修复、更新、重新安装与打开目录；
- [ ] 对外部安装或信息不完整的插件显示风险提示，不做静默删除；
- [ ] 将 `publish.xml` 操作改为结构化增删指定条目并补充回归测试。

### P1：权限管理模块

- [ ] 启动时只检测权限，不在无上下文时连续弹系统授权；
- [ ] 首次需要写入 WPS 目录时解释用途后申请权限；
- [ ] 用户拒绝后保留可恢复状态，提供“重新申请权限”和“查看开启方法”；
- [ ] macOS 提供系统设置跳转、目录重新选择和逐步指引；
- [ ] Windows 提供目录访问检查、管理员权限说明和重新执行入口；
- [ ] 权限变化后自动复检，并把诊断信息做脱敏处理。

### P1：用户手册

- [ ] 编写快速开始：添加/选择控件源、安装日期选择器、重启 WPS、验证安装；
- [ ] 编写插件管理：查看状态、更新、修复、批量卸载和风险说明；
- [ ] 编写自定义源：索引格式、添加步骤、测试连接、启停、优先级和安全建议；
- [ ] 编写权限指南：macOS 与 Windows 分平台截图/步骤；
- [ ] 编写故障排除：无网络、下载失败、校验失败、WPS 版本不兼容、权限被拒绝、插件残留；
- [ ] 编写维护者指南：发布新控件版本、生成校验值、更新源索引和回滚；
- [ ] 在应用内增加“帮助与反馈”入口，并链接随应用发布的离线手册。

### P2：验收与发布

- [ ] 单元测试覆盖源解析、缓存、校验、插件发现、结构化卸载和权限状态转换；
- [ ] macOS arm64/x86_64 与 Windows 完成在线安装、更新、修复、卸载和拒绝权限后恢复测试；
- [ ] 验证升级安装器不会丢失用户自定义源，也不会误删既有第三方插件；
- [ ] 验证键盘操作、焦点管理、对比度、屏幕阅读器标签和减少动态效果；
- [ ] 发布前完成默认源可用性检查、版本回滚演练和用户手册校对。

## P0：发布前验收

- [ ] **修复并验证 Windows 安装包构建**
  - GitHub Actions 中确认 WiX MSI 在启用 VBSCRIPT 后能产出 `.msi`；
  - 确认 NSIS 同时产出 `.exe`；
  - 在干净 Windows 环境完成安装、覆盖安装、卸载和 WPS 重启验证。

- [ ] **完成 macOS 真实环境验收**
  - 在真实 WPS 表格中确认安装或覆盖安装后 Ribbon 出现“日期选择器”；
  - 验证卸载后 WPS 能重新打开；
  - 在干净 macOS 用户账户中验证离线安装，无 Node、Homebrew 或系统 7z。

- [ ] **覆盖两种 macOS 架构**
  - 构建并验收 Apple Silicon arm64 `.dmg`；
  - 构建并验收 Intel x86_64 `.dmg`；
  - 记录各架构的验收结果和测试环境。

- [ ] **补齐 CI 质量门禁**
  - 在 CI 中执行 Rust fmt、clippy 和 test；
  - 保留前端 typecheck/build；
  - 为安装流程的关键失败场景保留可重复运行的测试记录。

- [ ] **验收 tag 发布流程**
  - 推送新的 `v*` tag；
  - 确认 GitHub Release 附带 macOS、Windows 和 Linux 安装包；
  - 确认用户侧下载路径只使用 Release 附件，Actions Artifacts 仅用于构建检查与排障。

## P1：发布可靠性与维护

- [ ] 完成 Apple Developer ID 签名、notarization 和正式分发配置；
- [ ] 决定卸载时是否定向清理 `publish.xml`，并补 XML 解析测试；
- [ ] 将安装时的 `publish.xml` 写入升级为只更新 date-picker、保留其他加载项；
- [ ] 增加安装前后差异预览和本地操作历史；
- [ ] 增加“仅重启 WPS”和“打开安装目录”辅助操作；
- [ ] 支持自动发现非标准 WPS 安装位置；
- [ ] 评估 universal binary，减少用户选择架构的成本；
- [x] 设计应用自动更新：Tauri Updater + GitHub Releases + minisign 签名；首个带更新器的版本发布后，后续版本可自动更新。

## 软件更新模块未完成事项

- [x] 配置 GitHub Secrets：`TAURI_SIGNING_PRIVATE_KEY`、`TAURI_SIGNING_PRIVATE_KEY_PASSWORD`；
- [x] 本地验证 macOS 签名产物：`npm run package:zip -- --target x86_64-apple-darwin` 产出应用压缩包和 `.sig`。
- [x] 提交改动并发布首个 updater 版本 v0.0.10：Actions 构建成功，Release 资产齐全，`latest.json` 平台/签名/URL 正确；
- [x] 验证真实自动更新：v0.0.10 成功检测并更新到 v0.0.11，下载、验签、安装、重启均通过。
- [ ] macOS Apple Developer ID 签名与公证接入发布 workflow。

## P2：扩展能力

- [ ] **完成 Windows 真实环境验收**
  - [x] 实现 WPS 探测（注册表 InstallRoot / LOCALAPPDATA 扫描）、版本检查、jsaddons 部署与进程重启；
  - [ ] 在干净 Windows 环境完成安装、覆盖安装、卸载和 WPS 重启验证；
  - [ ] 确认个人版 WPS 上 publish.xml 流程加载正常。
- [ ] 支持多个 WPS 加载项及多版本切换；
- [ ] 支持企业离线包、静默安装参数和管理日志导出；
- [ ] 国际化与无障碍专项验收。

## 发布完成定义

满足以下条件后，首个正式版本才可发布：

1. macOS arm64 与 x86_64 均通过真实 WPS 验收；
2. Windows 的 MSI 与 NSIS 安装包均成功构建并完成基础安装验收；
3. 安装、覆盖安装和卸载可重复执行，且不会删除 `jsaddons` 中无关内容；
4. tag workflow 创建的 GitHub Release 包含所有目标平台的安装包；
5. Release 下载的产物完成签名与公证要求，或明确标记为未签名测试版。
