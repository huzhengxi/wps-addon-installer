# 管理插件

## 查看状态

“插件”页面的“已安装”区域显示名称、版本、来源和健康状态。状态为“需要修复”时，选择该插件并执行重新安装；状态为“外部安装”时，安装器会在卸载前额外提示风险。

## 卸载插件

1. 勾选一个或多个已安装插件。
2. 点击列表下方出现的“卸载”。
3. 核对确认窗口中的数量后点击“卸载”。

卸载仅移除选中插件的目录及对应的 WPS `publish.xml` 条目，不会删除其他插件。

## 更新和修复

来源刷新后，如果有新版本，插件行会显示更新操作。下载的文件必须通过 SHA-256 校验；校验失败时安装器会删除下载文件并阻止安装。
