{
  "name": "wps-addon-installer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "tauri": "tauri",
    "check": "npm run build",
    "sync:addon": "node scripts/sync-addon-resources.mjs --skip-build"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "typescript": "5.7.3",
    "vite": "4.5.14"
  }
}
